      - name: Run Build
        run: |
          echo "=== Starting Build ==="
          python -u build.py
          echo "=== Build Finished ==="
          dir dist 2>&1 || echo "dist folder NOT FOUND"
          echo "=== Checking current directory ==="
          dir
